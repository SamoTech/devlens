# GitHub OAuth (NextAuth)
AUTH_SECRET=
GITHUB_ID=
GITHUB_SECRET=

# Optional: server-side GitHub token (increases rate limit to 5000/hr)
GITHUB_TOKEN=

# Upstash Redis — for caching + real trend history
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
