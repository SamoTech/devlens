# GitHub OAuth — create at https://github.com/settings/applications/new
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Auth.js secret — run: openssl rand -base64 32
AUTH_SECRET=

# Optional: higher GitHub API rate limit (60 → 5000 req/hr)
GITHUB_TOKEN=
